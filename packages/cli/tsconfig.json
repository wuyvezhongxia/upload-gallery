{
  "compilerOptions": {
    "target": "ES2020", // 编译后的 JS 语法版本，适配 Node.js 较新版本特性
    "module": "ESNext", // 模块系统，支持 ES 模块语法
    "moduleResolution": "Node", // 按照 Node.js 方式解析模块
    "outDir": "dist", // 编译后文件输出目录，可自定义
    "rootDir": "src", // 待编译的 TS 文件根目录，若脚本在根目录可按需调整
    "strict": true, // 开启严格模式，加强类型校验
    "esModuleInterop": true, // 支持默认导入 CommonJS 模块
    "skipLibCheck": true, // 跳过声明文件（.d.ts）的类型检查，加快编译
    "forceConsistentCasingInFileNames": true // 文件名大小写严格匹配
  },
  "include": ["src/**/*.ts", "packages/cli/TS/index.ts"], // 指定要编译的 TS 文件，根据你实际脚本路径调整
  "exclude": ["node_modules", "dist"] // 排除不需要编译的目录
}
